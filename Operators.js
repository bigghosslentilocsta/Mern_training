let totalAmount = 0;

//add 500 to total

totalAmount += 500;
console.log("Total Amount after adding 500:", totalAmount);

//add 1200 to total

totalAmount += 1200;
console.log("Total Amount after adding 1200:", totalAmount);

//apply discount of 200

totalAmount -= 200;
console.log("Total Amount after applying discount of 200:", totalAmount);

//add 18% gst
totalAmount += totalAmount * 0.18;
console.log("Total Amount after adding 18% GST:", totalAmount);

console.log("Final Total Amount:", totalAmount);
